# Write a method `calculate_area` in a class `Circle` that takes the radius as a parameter and uses a local
# variable to calculate and return the area of the circle.
class Circle:
    def Calculate_Area():
        r=int(input("Enter Value for r"))
        area=3.142*r*r
        print(area)
c1=Circle
c1.Calculate_Area()

